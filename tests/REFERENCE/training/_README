Step 0:
Choose strain set for training (called training_strain_set.dat)
$ cp _training_strain_sets/training_strain_set.dat.41 _training_strain_set.dat

Step 1:
Create trajectory directories
$ python _0_make_trajectories.py


Step 2:
If running in local desktop:
$ bash _1a_run_trjectories.bash

If running on acuario:
$ sbatch 1b_slurm_trajectories.bash
you need to set the slurm options, accordingly to the case.

Step 3:
After computing cases, mv cases no to be included in training to "validation" directory:
$ mv trajectory_40 validation
